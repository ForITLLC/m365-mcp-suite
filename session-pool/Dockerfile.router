FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    curl \
    docker.io \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

RUN pip install --no-cache-dir flask httpx docker

COPY router.py .

EXPOSE 5200

CMD ["python", "router.py"]
